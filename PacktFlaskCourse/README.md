Packt Flask Course - Includes a bunch of interesting assignments and applications!!!

# DO EVERY TIME (LOCAL ONLY)
cd ../Sukrit Ganesh/Documents/LearningProgramming/Python/PacktFlaskCourse/food_app
conda activate packt_flask_env
set FLASK_DEBUG=1
flask run
conda deactivate

git add .
git commit -m "<message here>
git push origin master